<%= link_to("Back to Cohorts", cohorts_path, :class => 'back-link') %>

<%= bootstrap_form_for @cohortstudent do |f| %>
  <%= f.number_field :cohort_id %>
  <%= f.number_field :student_id %>
  <%= f.submit("Add Student") %>
<% end %>
